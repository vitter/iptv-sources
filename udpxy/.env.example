# 环境变量配置文件示例
# 复制此文件为 .env 并填入你的实际配置

# FOFA 配置（必须）
FOFA_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36
FOFA_COOKIE=你的FOFA_Cookie字符串

# FOFA API 密钥（可选，如果有则优先使用API方式）
FOFA_API_KEY=你的FOFA_API密钥

# Quake360 配置（必须）
QUAKE360_TOKEN=你的Quake360_Token

# ZoomEye 配置（可选）
ZOOMEYE_API_KEY=你的ZoomEye_API密钥

# 配置说明：
# 1. FOFA_COOKIE 是必须的，用于Cookie认证方式
# 2. FOFA_API_KEY 是可选的，如果配置了则优先使用API方式
# 3. QUAKE360_TOKEN 是必须的，Quake360只使用Token认证
# 4. ZOOMEYE_API_KEY 是可选的，如果配置了则启用ZoomEye搜索
# 5. 所有配置值都不要加引号
